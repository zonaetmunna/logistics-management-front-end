// product interface
interface IProduct {
    _id?: string;
    name: string;
    category: string;
    description: string;
    image: string;
    price: number;
    store: string;
}

interface BrandData {
    name: string
    email: string
    description: string
    location: string
    status: string
}
